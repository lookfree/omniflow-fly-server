# Fly Server Environment Variables

# Data directory for project files
DATA_DIR=/data/sites

# Server port
PORT=3000

# Bun binary path (default: use process.execPath)
# BUN_BINARY=/usr/local/bin/bun

# JSX Tagger dependency path
# JSX_TAGGER_DEP=file:/app/packages/vite-plugin-jsx-tagger

# Fly.io public host for HMR WebSocket connections
# FLY_PUBLIC_HOST=ai-site-preview.fly.dev

# Enable HTTPS for HMR (set to 'true' on fly.dev)
# FLY_HTTPS=true

# Fly.io API Security (spec0007)
# Generate secure keys using: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
FLY_API_KEY=fly_sk_your_api_key_here
FLY_API_SECRET=fly_secret_your_secret_here
